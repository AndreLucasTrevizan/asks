version: '3.7'

services:
  asksapi:
    build: './api'
    container_name: asksapi_container
    volumes:
      - ./:/home/Documents/asks
    command: npm start
    ports:
      - '8080:8080'
    networks:
      - backend
  asksdb:
    image: mysql
    container_name: asksdb_container
    environment:
      MYSQL_DATABASE: asksdb
      MYSQL_ROOT_PASSWORD: asdbpassword
    volumes:
      - './database/:/var/lib/mysql'
    ports:
      - '9706:3306'
    networks:
      - backend

networks:
  backend:
    driver: bridge